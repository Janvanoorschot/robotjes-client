#!/usr/bin/env python

from pprint import pprint

def execute(robo):

    status = robo.forward()
    pprint(status)
    status = robo.forward()
    pprint(status)
    status = robo.forward()
    pprint(status)
    status = robo.forward()
    pprint(status)
    status = robo.left()
    pprint(status)
    status = robo.forward()
    pprint(status)
    status = robo.forward()
    pprint(status)
    status = robo.forward()
    pprint(status)
    status = robo.forward()
    pprint(status)
    status = robo.forward()
    pprint(status)
    status = robo.right()
    pprint(status)
    status = robo.forward()
    pprint(status)
    status = robo.forward()
    pprint(status)
    status = robo.forward()
    pprint(status)
    status = robo.forward()
    pprint(status)
    print("last Move")
    status = robo.forward()
    pprint(status)
    print("pickUp")
    status = robo.pickUp()
    pprint(status)
    print("stop")
    # status = robo.stop()
    # pprint(status)

