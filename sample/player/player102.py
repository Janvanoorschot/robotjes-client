#!/usr/bin/env python

from pprint import pprint

def execute(robo):

    status = robo.forward()
    status = robo.forward()
    status = robo.forward()
    status = robo.forward()
    status = robo.left()
    status = robo.forward()
    status = robo.forward()
    status = robo.forward()
    status = robo.forward()
    status = robo.forward()
    status = robo.right()
    status = robo.forward()
    status = robo.forward()
    status = robo.forward()
    status = robo.forward()
    status = robo.forward()
    status = robo.pickUp()
    # status = robo.stop()

