

def execute(robo):
    while True:
        reply = robo.forward(2)
        reply = robo.right()
        reply = robo.forward()
        reply = robo.left()
        reply = robo.forward(2)
        reply = robo.left(2)
        reply = robo.forward(2)
        reply = robo.right()
        reply = robo.forward()
        reply = robo.left()
        reply = robo.forward(2)
        reply = robo.left(2)

