'use strict';
(function($) {

    $.fn.robotjes.robotjesdashboard = function(node) {
        let that = {};

        that.node = node;

        that.genmon = function(type, msg, args) {
        }

        somefunc(that);

        return that;
    }

    function somefunc(that) {
    }

})(jQuery);